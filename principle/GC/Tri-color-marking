Go本地缓存 BigCache

  如何不被垃圾回收 :
                  1.只有堆上的才会被回收
                  2.GC对非指针对象不作操作（保证对象是值类型不被回收，map中值类型和指针类型被忽略） 
                  
  三色标记法（增量式GC算法）：
                          1.白色：未搜索的对象
                          2.灰色：搜索中的对象
                          3.黑色：搜索完成的对象
                          
  搜索过程：
              首先GC认为所有对象都是白色,将能找到的对象进行标记放入栈中,此时把从根扫描的对象标记为灰色,对子对象进行重复标记,待全部完成后,
          将灰色对象标记为黑色,此时只有白和黑,被标记为黑色的对象不被扫描,将白色对象清除.
          
  写屏障(Dijkstra算法)：创建或改变指针过程中,要把创建或改变的标记为灰色(例：当GC停止过程中黑色对象指向了白色对象,若无写屏障,再次执行GC时白色会被清除,有
         写屏障则将白色变为灰色使之仍可继续被扫描到,不被清除.)
          
